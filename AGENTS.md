The code language is English. The language of the interface is Italian.

You can check the enode documentation at this url: https://developers.enode.com/api/reference

The code should be minimal and clear. Introduce libraries only if needed and always ask permission before.

## uv
The project uses uv library.
Use uv add for new libraries and uv run for running the "main" scripts.

## Debug files
If you write any debug files, save them in Debug folder. Delete them after some time.

## DATABASES
For users, the main key to be used for identifying a specific is user_id, do not use telegram_id if not strictly necessary.
